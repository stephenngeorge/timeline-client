import React from 'react'

// import child components
import Nav from './Nav'

const Header: React.FC = () => {
    return (
        <div>
            <Nav />
        </div>
    )
}

export default Header